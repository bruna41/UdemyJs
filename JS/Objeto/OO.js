// CÓDIGO NÃO EXECUTÁVEL

// Procedural; foco é a função
processamento (v1, v2, v3)

// OO; dados com funções
objeto = {
    v1,
    v2,
    v3,
    processamento () {
        // ...
    }
}
objeto.processamento()

/*Principios:
 * Abstração
 * Encapsulamento->detalhes internos
 * Herança (prototype)
 * Polimorfismo */